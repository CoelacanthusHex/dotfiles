#!/bin/bash -e

#
# SPDX-FileCopyrightText: 2020 lilydjwg
#


cmd="$@"
unshare -m bash <<EOF
mount --make-rprivate /
for f in /etc/pacman.d/*.sync; do
  filename="\${f%.*}"
  mount --bind "\$f" "\$filename"
done
$cmd
EOF
